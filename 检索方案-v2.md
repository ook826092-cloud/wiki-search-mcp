# LLM Wiki 检索系统设计方案 v2

> 版本：v2.0 ｜ 日期：2026-08-06 ｜ 状态：待审阅
> 核心原则：**md 原件不动，DB 只做索引副本；复杂度刚好合适；零 API、零向量、零本地大模型**

---

## 1. 我们到底要做到什么样

一句话：**给 LLM Wiki 做一个"程序化的智能检索入口"**——AI 通过 MCP 传一句话就能搜到想要的笔记（含附件线索），毫秒级，不遍历文件、不消耗 token 输命令。

```
你/AI 提问 → MCP 工具（search/get/search_attachment…）
          → SQLite FTS5 毫秒级命中
          → 返回相关页面 + 命中摘要 + 附件线索
          → AI 读原 md 综合回答
```

## 2. 架构总览

```
┌────────────────────────────────────────────────────────┐
│  AI（Claude Code / Codex / 助手）                        │
│    │ MCP 调用（只传参数）                                │
│    ▼                                                    │
│  FastMCP Server「wiki-search」（Termux 常驻）             │
│    ├─ search              检索页面                        │
│    ├─ get                 读页面全文                      │
│    ├─ search_attachment   检索附件（聪明之处④）            │
│    ├─ get_attachment      取附件线索                      │
│    ├─ list_pages / status / reindex                      │
│    ▼                                                    │
│  SQLite ~/wiki-search/wiki.db                            │
│    ├─ pages_fts       FTS5 全文索引（trigram）            │
│    ├─ page_meta       页面元数据                          │
│    ├─ attachments     附件清单（文件名/路径/类型）          │
│    ├─ attachment_links 页面↔附件 引用关系（聪明之处③）      │
│    └─ meta            索引状态（增量用）                   │
│    ▲                                                    │
│    数据源：/sdcard/Obsidian/Obsidian/wiki/**              │
└────────────────────────────────────────────────────────┘
```

## 3. 检索引擎：SQLite FTS5

- **零安装**：Termux Python 3.14 自带 SQLite 3.53.4（FTS5 + trigram 已验证）
- **BM25 排序**：工业级算法（Elasticsearch 同款）
- **中文**：trigram（≥3字）+ LIKE 回退（<3字），已实测
- **列权重**：标题命中权重 10 倍于正文
- **snippet**：命中摘要 + 高亮标记，AI 不用读全文

## 4. 数据库设计（5 张表）

```sql
-- ① 全文索引（FTS5，trigram）
CREATE VIRTUAL TABLE pages_fts USING fts5(
    path, title, body, tags, tokenize='trigram');

-- ② 页面元数据
CREATE TABLE page_meta(
    path TEXT PRIMARY KEY, title TEXT,
    page_type TEXT, tags TEXT, size INT, updated REAL);

-- ③ 附件清单（聪明之处①：不存二进制，只存路标）
CREATE TABLE attachments(
    path TEXT PRIMARY KEY, filename TEXT,
    ext TEXT, size INT, updated REAL);

-- ④ 页面↔附件 引用关系（聪明之处③：从 md 正文解析 ![[...]] 得出）
CREATE TABLE attachment_links(
    page_path TEXT, att_path TEXT,
    PRIMARY KEY(page_path, att_path));

-- ⑤ 索引状态（聪明之处⑤：增量索引用）
CREATE TABLE meta(key TEXT PRIMARY KEY, value TEXT);
-- 存：last_reindex、每个文件的 mtime 快照
```

## 5. MCP Server：7 个工具

| 工具 | 参数 | 用途 |
|---|---|---|
| `search` | query, limit, page_type | 检索页面（BM25+snippet+列权重） |
| `get` | path, max_lines | 读页面全文 |
| `search_attachment` | query, ext | 搜附件（文件名/路径） |
| `get_attachment` | path | 返回附件线索（路径/大小/类型） |
| `list_pages` | page_type | 页面清单 |
| `status` | — | 索引状态 |
| `reindex` | — | 全量/增量重建索引 |

## 6. 高级功能清单（分阶段）

### v1 核心（现在做）
- [x] FTS5 trigram + LIKE 回退（中文）
- [x] BM25 相关度排序
- [x] **列权重**：标题 10x 正文
- [x] **snippet** 命中摘要 + 高亮
- [x] **元数据过滤**：按 page_type / tags 先筛后搜
- [x] **附件 L1**：md 内 `![[图.png]]` 引用自动进索引（免费获得）
- [x] **附件 L2**：attachments 表 + search_attachment 工具

### v2 增强（库大了再上）
- [ ] **双表策略**：英文 unicode61 + 中文 trigram，按语言路由
- [ ] **增量索引**：mtime 快照，只重建变动文件（秒级）
- [ ] **附件关联反查**：搜附件 → 反查引用它的页面（聪明之处③完整版）
- [ ] **附件类型过滤**：ext 参数（图/PDF/音视频分开搜）

### v3 可选
- [ ] 触发器自动同步（SQLite 内部联动）
- [ ] contentless 索引瘦身（DB 减 60%）
- [ ] 查询扩展（同义词表，伪语义）

## 7. 附件处理的 5 个聪明之处（重点）

| # | 聪明之处 | 原理 | 效果 |
|---|---|---|---|
| ① | **不复制，只存路标** | 附件二进制不进 DB（几百张图=几百MB），只存 文件名/路径/类型/大小 | DB 保持几 MB，检索快 |
| ② | **引用自动入索引** | md 正文里的 `![[架构图.png]]` 本身在 body 里，被 FTS5 索引 | 搜"架构图"免费命中含图页面，零额外工作 |
| ③ | **引用关系双向映射** | 解析所有 md 的 `![[...]]` 引用 → attachment_links 表 | 搜到附件能反查引用它的页面；看页面能列出它引用的附件 |
| ④ | **附件可被直接检索** | attachments 表 + search_attachment 工具 | 搜"架构图"直接命中图片文件，不只是页面 |
| ⑤ | **中文附件名友好** | 附件名走 trigram（中文 3 字+）+ LIKE 回退（<3 字） | "设计草图.png" 能搜"设计草图" |

**附件的完整生命周期**：
```
Obsidian 附件文件夹（原件，永不动）
    │ reindex 时
    ├─ md 里的 ![[引用]] → 自动进 pages_fts（聪明②）
    ├─ 附件清单 → attachments 表（聪明①）
    └─ 引用关系 → attachment_links 表（聪明③）

检索时：
  搜"架构图" → ① 命中含图页面  ② 命中附件文件本身
  → get_attachment 返回路径 → Obsidian 直接打开
```

## 8. 实施步骤

| 阶段 | 内容 | 产出 |
|---|---|---|
| P1 | 写 server.py（含建库/建表/导入/检索/MCP） | 7 个工具可调用 |
| P2 | pip install fastmcp，reindex 建库 | 检索可用 |
| P3 | 启动 MCP（HTTP + stdio），实测各工具 | 全流程验证 |
| P4 | 接入助手检索流程 + 更新 skill | 日常可用 |

## 9. 验收标准

- [ ] search("收藏夹")、search("搜索")（<3字）均正确命中
- [ ] 命中带 snippet 摘要 + 相关度排序
- [ ] search_attachment("架构图") 能搜到附件
- [ ] 附件↔页面 引用关系可双向反查
- [ ] 图片等附件原件不受影响（仍在 Obsidian 附件文件夹）
- [ ] MCP HTTP 外部可调（POST /mcp）
- [ ] reindex 支持增量（mtime 判断）

---

## 10. 待确认

1. v1 的 6 项功能 + 附件 5 聪明之处，覆盖度够吗？
2. 附件：Obsidian 的附件文件夹在哪？（`wiki/attachments/`？还是 vault 根目录？）——决定 attachments 表扫描范围
3. MCP 传输：stdio + HTTP 都要，还是先 HTTP（给我用）？
