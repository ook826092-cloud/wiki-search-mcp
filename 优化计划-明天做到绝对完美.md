# wiki-search 优化计划（明天做到绝对完美）

> 版本：计划 v1.0 ｜ 日期：2026-08-06 ｜ 状态：待执行
> 当前基线：**v2.0.0 生产就绪**（9 个 MCP 工具 / 15.3MB / 1774 页 / 421 附件 / 179 引用）

---

## 📊 当前能力快照

| 能力 | 状态 |
|---|---|
| 检索 | jieba 词级 + 降级 OR + LIKE 兜底（13-44ms） |
| 过滤 | page_type + tags 双重过滤 |
| 定位 | `abs_path` + `obsidian_url`（直达原文件） |
| 摘要 | 命中词高亮 ⟪⟫ + `preview` 轻量预览 |
| 同步 | 懒自动（60s 检测，写新文件即搜） |
| 附件 | 双向映射（search_attachment / page_attachments） |
| 健康 | status / reindex / schema 版本 |

**目标：明天把查询做到"绝对完美"，补齐所有值得做的优化。**

---

## 一、优先项（查询体验核心）

### 1️⃣ 时间过滤（search 加 `since` / `updated` 参数）

- **价值**：⭐⭐⭐「最近 7 天写了什么」「上周的笔记」——日记/日志类场景刚需
- **实现**：`search(..., since="7d")` → page_meta.updated 过滤（SQL WHERE updated >= now-7d）
- **成本**：10 行，改 search + list_pages

### 2️⃣ related 工具（推荐相关页）

- **价值**：⭐⭐⭐ 知识图谱探索——搜到一个页面，自动带出关联页（Karpathy 交叉引用精神）
- **实现**：新工具 `related(path, limit)` → 按共享 tags + wikilink 关联度推荐
- **成本**：30 行

### 3️⃣ search 返回总数（`total` 字段）

- **价值**：⭐⭐ 知道"一共命中多少"（现在只看到前 N 条）
- **实现**：search 返回加 `total`（COUNT 查询）
- **成本**：5 行

### 4️⃣ 开机懒启动（MCP 连不上自动拉起）

- **价值**：⭐⭐⭐ 手机重启后第一次用 MCP，SSH 自动拉起 server（开机自启的务实替代/兜底）
- **实现**：客户端侧逻辑（AI 调 MCP 失败 → SSH 检查 pgrep → nohup 启动 → 重试）
- **成本**：写进 skill 引导即可，零代码

---

## 二、推荐项（锦上添花）

### 5️⃣ 同义词扩展

- **价值**：⭐⭐ 伪语义召回——搜"存起来"→自动带"收藏夹 书签"
- **实现**：小词表 SYNONYMS，查询时扩展 jieba 词
- **成本**：15 行 + 词表维护

### 6️⃣ 附件懒同步补全

- **价值**：⭐⭐ 懒同步现在只查 md，补上附件变化检测（60s 间隔）
- **实现**：_maybe_sync 加附件 stat 检查（约 +1.3s/次，可接受）
- **成本**：10 行

### 7️⃣ DB 备份工具（`backup`）

- **价值**：⭐⭐ 一键备份 wiki.db，防损坏
- **实现**：新工具 backup → cp wiki.db 到指定位置（vault/backup 目录）
- **成本**：10 行

---

## 三、可选/未来

| # | 项 | 说明 |
|---|---|---|
| 8 | 分页 offset 参数 | search/list_pages 翻页 |
| 9 | 热门搜索统计 | 记录 query 日志，洞察使用 |
| 10 | VACUUM 自动提示 | DB 碎片大时提示维护 |
| 11 | 超大页分块 | 单页 >1MB 时按 `#` 标题切块（当前截断保护已够） |
| 12 | 向量语义检索 | sqlite-vec + API 嵌入（已评估，暂缓——jieba 词级已够） |

---

## 🗓️ 明天执行顺序

```
1. ① 时间过滤（since 参数）        —— 10 分钟
2. ② related 工具                  —— 30 分钟
3. ③ search total 字段             —— 5 分钟
4. ④ 开机懒启动（skill 引导）       —— 10 分钟
5. ⑤ 同义词扩展                    —— 20 分钟（可选）
6. ⑥ 附件懒同步补全                —— 10 分钟（可选）
7. ⑦ backup 工具                   —— 10 分钟（可选）
8. 全量回归测试 + skill 更新         —— 30 分钟
```

**验收标准**：
- [ ] `search("深度学习", since="7d")` 只返回近 7 天更新的页面
- [ ] `related(path)` 返回关联页（共享 tags / wikilink）
- [ ] search 返回带 `total`
- [ ] MCP 连不上时自动拉起（skill 引导）
- [ ] 全部功能回归无退化
- [ ] skill 同步更新（工具表/工作流）

---

*写完推送到 Obsidian，明天照此执行 💪*
