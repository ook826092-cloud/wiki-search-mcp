# LLM Wiki 检索方案（纯 SQLite + MCP Server）

> 版本：v1 ｜ 日期：2026-08-06 ｜ 状态：待审阅

---

## 1. 背景

手机 Obsidian vault 中已有 LLM Wiki（Karpathy 模式），知识库会持续增长（当前 wiki/ 6 页，未来数百页）。
检索是核心痛点：**"检索才是问题，遍历文件才是问题"**。
需要一个**不复杂也不简单、刚好合适**的检索方案。

## 2. 目标与原则（已与用户确认）

| 原则 | 说明 |
|---|---|
| ❌ 不调用 API | 不用嵌入/重排 API |
| ❌ 不搞向量引擎 | 不用向量数据库/向量检索 |
| ❌ 不搞本地大模型 | 不下载 GGUF 模型 |
| ✅ 纯 SQLite | 用 SQLite 原生 FTS5 全文检索（Termux Python 自带，零安装） |
| ✅ 必须有 MCP Server | 程序化检索，AI 只传参数，不消耗 token 输命令 |
| ✅ 复杂度刚好 | 比"grep 随便搜"强（相关度排序+中文+摘要），比"完整 RAG 管线"简单（零 API 零向量） |

## 3. 架构总览

```
┌─────────────────────────────────────────────────────┐
│  AI（Claude Code / Codex / 我）                       │
│    │  通过 MCP 调用（传参数，不输命令）                 │
│    ▼                                                 │
│  FastMCP Server（wiki-search，Termux 常驻）            │
│    │  工具：search / get / list_pages / status / reindex│
│    ▼                                                 │
│  SQLite（~/.wiki-search/wiki.db）                     │
│    ├─ pages_fts   ← FTS5 全文索引（trigram，中文友好） │
│    └─ page_meta   ← 页面元数据（标题/类型/标签）        │
│    ▲                                                 │
│    │  数据源：/sdcard/Obsidian/Obsidian/wiki/**/*.md   │
└─────────────────────────────────────────────────────┘
```

## 4. 检索引擎设计（SQLite FTS5）

### 4.1 为什么选 FTS5

- SQLite 内置（Termux Python 3.14 自带，SQLite 3.53.4，**零安装零依赖**）
- BM25 相关度排序（成熟算法）
- trigram tokenizer 支持中文（已验证）
- 毫秒级检索（几百页规模）

### 4.2 表结构

```sql
-- 全文索引表（trigram：中文按 3 字 n-gram，支持子串匹配）
CREATE VIRTUAL TABLE pages_fts USING fts5(
    path, title, body, tags,
    tokenize='trigram'
);

-- 元数据表
CREATE TABLE page_meta(
    path TEXT PRIMARY KEY,     -- wiki 内相对路径
    title TEXT,                -- frontmatter title
    page_type TEXT,            -- source / concept / entity / report / note
    tags TEXT,                 -- frontmatter tags
    size INT, updated REAL
);
```

### 4.3 中文检索策略（已实测验证）

| 查询长度 | 策略 | 实测结果 |
|---|---|---|
| ≥3 字中文 | FTS5 trigram MATCH（BM25 排序） | ✅ "收藏夹"→ 命中 |
| <3 字中文 | LIKE 回退（标题/路径） | ✅ "搜索"→ 命中 |
| 短拉丁词 | LIKE 回退 | ✅ |

> 已知缺陷与对策：trigram 对 <3 字符的词无法索引（官方限制），故用 LIKE 回退补齐召回，两者合并去重后按分数排序。

### 4.4 备选：Rust 方案（不采用，但记录）

- 可用 Rust 重写引擎（`rusqlite` + FTS5），Termux 对 Rust 支持好
- 收益：单一原生二进制、无 Python 运行时
- 成本：编译时间、维护复杂度
- **结论：当前 Python + SQLite 已满足"刚好合适"，Rust 留作未来优化项**

## 5. MCP Server 设计（FastMCP）

### 5.1 工具清单

| 工具 | 用途 | 参数 |
|---|---|---|
| `search` | 检索相关页面 | `query`(str,必填), `limit`(int,默认10), `page_type`(str,可选) |
| `get` | 读页面全文 | `path`(str,必填), `max_lines`(int,默认200), `from_line`(int,默认0) |
| `list_pages` | 页面清单 | `page_type`(str,可选) |
| `status` | 索引状态 | 无 |
| `reindex` | 重建索引 | 无 |

### 5.2 search 返回结构

```json
[
  {
    "path": "concepts/content-curation.md",
    "title": "内容策展",
    "page_type": "concept",
    "score": 0.87,
    "match_kind": "fts"        // fts=trigram命中 / like=回退命中
  }
]
```

### 5.3 传输方式

- **stdio**：Codex / Claude Code 原生子进程接入（`fastmcp run server.py`）
- **HTTP**：`fastmcp run server.py --transport http --port 8181`，供外部调用（`POST /mcp`）

## 6. 部署结构（Termux）

```
~/wiki-search/
├── server.py          ← MCP Server（约 150 行）
├── wiki.db            ← SQLite 索引（reindex 生成）
└── run.sh             ← 启动脚本（HTTP 常驻）
```

- 数据源：`/sdcard/Obsidian/Obsidian/wiki/**/*.md`
- 权限：Termux 需 storage 权限访问 /sdcard

## 7. 实施步骤（分 3 阶段）

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P1 引擎** | 装 fastmcp、写 server.py、reindex 建索引 | search/get 可用 |
| **P2 服务** | 启动 MCP（stdio + HTTP）、curl 实测 | MCP 工具可调用 |
| **P3 接入** | 接到我的 Query 工作流 + skill 更新 | AI 检索即用 |

## 8. 验收标准

- [ ] `search("收藏夹")`、`search("搜索")`（<3字）均命中正确页面
- [ ] `get` 返回页面 markdown 全文
- [ ] MCP HTTP 模式可从外部 curl 调用（POST /mcp）
- [ ] reindex 后新增页面可被检索
- [ ] 中文、英文混合查询均可用

## 9. 未来升级路径（明确不做，仅记录）

| 触发条件 | 升级内容 |
|---|---|
| 遇到"搜意思搜不到"（语义相似但字面无关）的痛点 | 加 sqlite-vec（官方安卓 .so）+ 嵌入 API → 语义召回 |
| 检索结果排序不理想 | 加重排 API（如 Jina/Cohere rerank） |
| 需要 AI 原生工具集成 | 已内置于 MCP（无需升级） |

> 原则：**每一步独立可加装，不推倒重来**。当前阶段 0 API、0 向量、0 大模型。

---

## 10. 待确认项

1. 方案是否 OK？有无要调整的地方（工具集/参数/部署位置）？
2. MCP 主要给谁用：只给我（SSH 调 HTTP）？还是也要给 Codex/Claude Code（stdio）？—— 决定传输方式优先级
3. 数据库文件位置：Termux 内（推荐，快）还是 /sdcard（慢但可备份）？
